# 玩具坦克 ESP32 MQTT 通信协议

> 适用固件：`legotank_esp32/legotank_esp32.ino`

## 1. 控制指令（上位机 → ESP32）

**Topic：** `tank/cmd`

**格式：** JSON，UTF-8 文本

### 字段说明

| 字段 | 类型   | 取值范围      | 说明                                   |
| ---- | ------ | ------------- | -------------------------------------- |
| `L`  | 整数   | `-255 ~ 255`  | 左电机 PWM 占空比，负值倒车，正值前进  |
| `R`  | 整数   | `-255 ~ 255`  | 右电机 PWM 占空比，负值倒车，正值前进  |
| `M1` | 整数   | `0 ~ 180`     | 换挡舵机角度（0° ~ 180°）              |
| `M2` | 整数   | `0 ~ 255`     | 大灯电平值（直接 PWM 占空比，0=关闭，255=最亮）  |
| `M3` | 整数   | `0 ~ 255`     | 逗猫棒电平值（直接 PWM 占空比，0=关闭，255=最大）  |

- 所有字段均为**可选**；缺失的字段保持上一次值不变。
- 动力差转向、差速调速等逻辑必须在上位机完成，ESP32 直接使用原始值驱动硬件。
- M2/M3 使用直接 PWM 控制（频率 1200Hz，分辨率 8bit），可有效消除 LED 频闪。

### 示例

```json
{
  "L": 200,
  "R": -150,
  "M1": 90,
  "M2": 128,
  "M3": 0
}
```

含义：

- 左电机以 `200/255` 功率前进
- 右电机以 `150/255` 功率倒车
- M1 舵机回到中位
- M2 大灯电平 128（半亮）
- M3 逗猫棒电平 0（关闭）

### 仅控制舵机示例

```json
{
  "M1": 120,
  "M3": 255
}
```

电机保持当前状态不变。

---

## 2. 遥测数据（ESP32 → 上位机）

**Topic：** `tank/telemetry`

**格式：** JSON，发送间隔约 `500ms`（可在 `config.h` 中修改 `TELEMETRY_MS`）

### 字段说明

| 字段    | 类型   | 单位     | 说明                            |
| ------- | ------ | -------- | ------------------------------- |
| `vbat`  | 浮点数 | V        | 3S LiPo 电池电压                |
| `L`     | 整数   | `-255~255` | 当前左电机实际输出              |
| `R`     | 整数   | `-255~255` | 当前右电机实际输出              |
| `M1`    | 整数   | `0~180`  | 当前 M1 舵机角度                |
| `M2`    | 整数   | `0~255`  | 当前 M2 电平（大灯）             |
| `M3`    | 整数   | `0~255`  | 当前 M3 电平（逗猫棒）           |
| `rssi`  | 整数   | dBm      | WiFi 信号强度                   |
| `uptime`| 整数   | ms       | ESP32 运行时间                  |

### 示例

```json
{
  "vbat": 11.34,
  "L": 200,
  "R": -150,
  "M1": 90,
  "M2": 128,
  "M3": 0,
  "rssi": -45,
  "uptime": 123456
}
```

---

## 3. 安全机制

- **指令超时保护（Watchdog）：** 若 `1000ms` 内未收到任何 `tank/cmd` 消息，ESP32 会自动将左右电机置 0（停车），舵机保持原位。
- 该机制可防止上位机崩溃、网络中断或程序异常时坦克失控。

---

## 4. 快速测试

使用 `mosquitto_pub` 或任意 MQTT 客户端向 `tank/cmd` 发送：

```bash
mosquitto_pub -h 192.168.2.45 -t tank/cmd -m '{"L":200,"R":200}'
```

订阅遥测：

```bash
mosquitto_sub -h 192.168.2.45 -t tank/telemetry
```

> 请将 `192.168.2.45` 替换为实际 MQTT 服务器地址（在 `config.h` 中配置）。